{% liquid
  assign product_resource = closest.product
  if request.visual_preview_mode and product_resource == blank
    assign product_resource = collections.all.products.first
  endif
%}

<!-- DEBUG: type={{ product_resource.type }} category={{ product_resource.category.name }} waist={{ product_resource.metafields.custom.measurement_waist.value }} -->
<div class="size-guide" {{ block.shopify_attributes }}>
  {% render 'size-guide-tables', product: product_resource %}
</div>

{% schema %}
{
  "name": "Product measurements",
  "tag": null,
  "settings": [],
  "presets": [
    {
      "name": "Product measurements",
      "category": "t:categories.product"
    }
  ]
}
{% endschema %}
